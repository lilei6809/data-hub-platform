package io.datahub.platform.iamprovisioning.application.pipeline;

public enum IamProvisioningStep {
        ENSURE_ORGANIZATION,
        ENSURE_USER,
        ENSURE_ORGANIZATION_MEMBERSHIP,
        ENSURE_REALM_ROLE,
        ENSURE_USER_REALM_ROLE
}
